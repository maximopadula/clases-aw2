import { iniciarPaginaDetalle } from './modulos/cohetes/controlador.cohetes.js';

// Escuchar el evento de carga del DOM para inicializar los detalles del cohete seleccionado
document.addEventListener("DOMContentLoaded", iniciarPaginaDetalle);
