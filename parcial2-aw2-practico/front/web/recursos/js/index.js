import { iniciarPaginaPrincipal } from './modulos/cohetes/controlador.cohetes.js';

// Escuchar el evento de carga del DOM para inicializar la lista principal de cohetes
document.addEventListener("DOMContentLoaded", iniciarPaginaPrincipal);
