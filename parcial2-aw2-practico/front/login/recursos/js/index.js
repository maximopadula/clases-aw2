import { iniciarLogin } from './modulos/usuarios/controlador.usuarios.js';

// Escuchar el evento de carga del DOM para inicializar la logica del controlador de login
document.addEventListener("DOMContentLoaded", iniciarLogin);
