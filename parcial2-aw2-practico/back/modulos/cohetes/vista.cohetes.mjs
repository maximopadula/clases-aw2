// Retornar los datos limpios en la vista tal como lo indica el patron MVC
export function obtenerTodos(datos) {
    return datos;
}

// Retornar el dato individual limpio en la vista
export function obtenerUno(dato) {
    return dato;
}
